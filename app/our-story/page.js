import OurStoryClient from "./OurStoryClient";

export const metadata = {
  title: "Our Story | Maya",
  description:
    "How Maya started — from a tiny kitchen experiment to a craft muesli brand obsessed with clean ingredients, bold flavour and zero compromise.",
};

export default function OurStoryPage() {
  return <OurStoryClient />;
}
